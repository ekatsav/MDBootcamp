// When given:
// Fahrenheit, a value should be converted to Celsius
// Celsius, a value should be converted to Fahrenheit

// To obtain temperature in Celsius: (F - 32) / 9 x 5
// To obtain temperature in Fahrenheit: (C * 1.8) + 32

//PSEUDOCODE/PLAN

// 1. create a function which takes 2 arguments - f anmd c (of og temp) and numerical value
// 2. if 'c' do c to f conversion
// 3. if f do f to c conversion
// 4. throw errors for arguments that aren't c or f???
// 5. return result

function temperatureConverter(tempValue, tempScale) {
    convertedTemp = 1;
    
    return convertedTemp;
}

module.exports = temperatureConverter;

//Expected inputs: C, c, celsius etc. OR can standardise all input to lower case